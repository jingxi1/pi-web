# pi-web 定时计划任务模块 —— 需求与技术方案

## 一、背景与目标

pi-web 是 pi coding agent 的本地 Web UI。当前需要新增一个**定时计划任务模块**：按预设时间表自动调用模型执行一段提示词，并把结果通过邮件发出。

之所以在 pi-web 内自建，而不用 Claude Code 内置的 scheduled-tasks：使用方为第三方 Claude Code 客户端，没有内置的计划任务界面，那套机制不适用。因此改为在本项目里自建，完全自主掌控。

**核心流程（4 步）：**
1. 有一个界面可以制定计划，把提示词写进去；
2. 设定调度的具体时间安排；
3. 系统到点后自动调用后端模型执行该提示词；
4. 执行完成后把结果通过邮件发送出去。

设计基调：先做一个精简但完整可用的版本，不过度设计。

## 二、功能需求

| 编号 | 需求 | 说明 |
| --- | --- | --- |
| F1 | 任务管理界面 | 可新建、编辑、删除、启用/停用计划任务 |
| F2 | 提示词编辑 | 每个任务保存一段提示词文本 |
| F3 | 调度设定 | **友好预设**两种模式：① 每 N 分钟/小时；② 每天定点 HH:MM |
| F4 | 自动执行 | 到点后服务端自动调用模型执行提示词，无需人工/前端参与 |
| F5 | 结果邮件 | 执行完成后把结果文本发邮件；可按任务开关、可指定收件人 |
| F6 | 立即运行（Run now） | 手动触发一次，用于调试，返回即时结果 |
| F7 | 运行记录 | 记录最近运行时间、状态、结果摘要、下次运行时间 |

## 三、约束与非功能需求

- **调度方式**：友好预设选择器，**不引入 cron 库**，纯手写计算下次触发时间。
- **邮件**：复用现有 `notify.json` 的 SMTP 配置与 `nodemailer` 封装，**零新增依赖**。
- **持久化**：任务定义存为 JSON，沿用现有配置存储约定（`getAgentDir()` 目录）。
- **运行时**：应用为 `next start` 单进程长驻（非 serverless），可承载进程内调度器。
- **时区**：每天定点 HH:MM 按服务器本地时区计算。

## 四、技术方案

### 4.1 总体架构

```
前端面板 (ScheduledTasksConfig)
      │  fetch
      ▼
API 路由 (/api/scheduled-tasks)  ──►  JSON 存储 (getAgentDir()/scheduled-tasks.json)
                                         ▲
进程内调度器 (scheduler, 60s tick) ──────┘
      │ 到点触发
      ▼
执行器 (runner) ── startRpcSession → set_model → prompt → 等 agent_end → 取结果
      │ 执行完成
      ▼
邮件 (sendNotifyEmail, 复用 notify.json SMTP)

instrumentation.ts  ──►  启动时拉起调度器 + 从 JSON 恢复任务
```

### 4.2 数据模型

存储位置：`getAgentDir()/scheduled-tasks.json`（沿用 `notify.json` 约定）。

```ts
// lib/scheduled-tasks-types.ts
interface TaskSchedule {
  type: "interval" | "daily";
  everyMinutes?: number;   // interval：间隔分钟数
  time?: string;           // daily："HH:MM"（本地时区）
}
interface TaskRun { at: string; status: "success" | "error"; summary: string; }
interface ScheduledTask {
  id: string;
  name: string;
  prompt: string;
  cwd: string;                                            // 执行工作目录（必填）
  model?: { provider: string; modelId: string } | null;  // null = 用默认模型
  schedule: TaskSchedule;
  enabled: boolean;
  email: { enabled: boolean; to?: string };               // to 空则回退 notify.json 的 to
  lastRunAt?: string | null;
  lastStatus?: "success" | "error" | null;
  lastResult?: string | null;                             // 最近一次结果文本（截断存储）
  nextRunAt?: string | null;
  history?: TaskRun[];                                    // 仅保留最近约 20 条
}
interface ScheduledTasksFile { tasks: ScheduledTask[]; }
```

### 4.3 后端文件（新增）

1. **`lib/scheduled-tasks-types.ts`** —— 上述类型定义。
2. **`lib/scheduled-tasks-store.ts`** —— JSON 读写（参照 `lib/notify-config.ts`：`getPath` → `readTasks()` 带 fallback → `writeTasks()` 先 mkdir-p 再 `JSON.stringify(, null, 2)`）；另含纯函数 `computeNextRun(schedule, from): string` 计算下次触发（interval：`from + everyMinutes`；daily：今天或明天的 HH:MM）。
3. **`lib/scheduled-tasks-runner.ts`** —— `runTask(task): Promise<TaskRun>`：
   - `generateSessionId()` + `startRpcSession(id, "", task.cwd, undefined)`（`undefined` = 允许全部工具，任务靠提示词自我约束）；
   - 若指定 `task.model` → `wrapper.send({ type: "set_model", ... })`；
   - `wrapper.send({ type: "prompt", message: task.prompt })`；
   - 用 Promise 包裹：`wrapper.onEvent(listener)` 等待 `{ type: "agent_end" }`，随后取最终 assistant 文本（`get_last_assistant_text`）；加**超时保护**（如 10 分钟）；
   - 若 `task.email.enabled`：读 `readNotifyConfig().email`，缺失则跳过并记录；`sendNotifyEmail(cfg, { subject: "[定时任务] " + task.name, text: 结果 })`，收件人优先 `task.email.to`，否则用 `cfg.to`；
   - 返回 `{ at, status, summary }`；**并发保护**：runner 内维护"正在运行"的 id 集合，跳过重复触发。
4. **`lib/scheduled-tasks-scheduler.ts`** —— 模块级单例：`ensureScheduler()` 用 `globalThis` 守卫防止重复注册（应对 dev 热重载）；`setInterval` 每 60 秒轮询：读任务表 → 对每个 `enabled` 且 `nextRunAt <= now` 的任务调用 `runTask`，完成后更新 `lastRunAt/lastStatus/lastResult/history/nextRunAt` 并回写 JSON。
5. **`instrumentation.ts`**（项目根，新建）—— `export function register()` 中调用 `ensureScheduler()`，`next start`/dev 启动即拉起；启动时为缺少 `nextRunAt` 的任务补算（解决 Docker restart 内存态丢失）。

### 4.4 API 路由（新增）

参照 `app/api/models-config/route.ts`（`export const dynamic = "force-dynamic"` + try/catch → `NextResponse.json`）。

- **`app/api/scheduled-tasks/route.ts`**
  - `GET`：返回全部任务；
  - `POST`：新增（生成 id、算首个 `nextRunAt`）；
  - `PUT`：更新单个任务（改字段后重算 `nextRunAt`）。
- **`app/api/scheduled-tasks/[id]/route.ts`**
  - `DELETE`：删除；
  - `POST`：Run now（立即 `runTask` 并返回结果，用于调试）。

### 4.5 前端（新增 + 挂载）

- **`components/ScheduledTasksConfig.tsx`**（参照 `components/SkillsConfig.tsx` 的全屏 modal 骨架：左列表 + 右详情 + 新增态）：
  - 左侧：任务列表（名称、下次运行时间、启用开关）；
  - 右侧：表单 —— 名称、提示词(textarea)、cwd、模型（复用现有模型选择，留空 = 默认）、调度（interval/daily 单选 + 对应输入）、邮件开关 + 收件人、**Run now** 按钮、保存/删除；
  - 展示 `lastRunAt / lastStatus / lastResult`；
  - 数据经 `fetch("/api/scheduled-tasks", ...)`。
- **`components/AppShell.tsx`** 挂载（参照 Skills/Notify 的做法）：新增 `tasksConfigOpen` state、侧边栏入口按钮、底部条件渲染 `{tasksConfigOpen && <ScheduledTasksConfig onClose=... />}`。

### 4.6 复用的现成能力（不重写）

| 能力 | 复用对象 |
| --- | --- |
| 模型执行 | `startRpcSession` / `generateSessionId`（`lib/rpc-manager.ts`），用法见 `app/api/agent/new/route.ts` |
| 邮件发送 | `sendNotifyEmail`（`lib/email-sender.ts`）+ `readNotifyConfig`（`lib/notify-config.ts`）+ `NotifyEmailConfig`（`lib/notify-types.ts`） |
| 存储约定 | `getAgentDir()`（`@earendil-works/pi-coding-agent`） |

## 五、风险与注意点

- **无人值守执行**：任务提示词若包含 push 代码、发外部系统等不可逆动作，服务端会照常执行（不像交互会话有确认环节）。默认允许全部工具，需靠提示词自我约束；建议首个任务保持只读/起草。
- **Docker restart**：内存态丢失，依赖 `instrumentation.ts` 启动时从 JSON 恢复并补算 `nextRunAt`。
- **dev 热重载**：调度器必须用 `globalThis` 单例守卫，否则会重复注册多个 `setInterval`。
- **时区**：daily 的 HH:MM 按服务器本地时区计算。

## 六、验证方式

1. 静态检查：`node_modules/.bin/tsc --noEmit` 与 `npm run lint`（不要跑 build）。
2. 启动 dev：`npm run dev`（端口 30141），打开侧边栏新入口，新建任务（简单提示词、interval = 每 2 分钟、cwd 指向本仓库）。
3. 点 **Run now**：确认即时返回结果、`lastResult` 有值；若开启邮件且 `notify.json` SMTP 已配好，确认收到邮件。
4. 等待一个 interval 周期，确认调度器自动触发、`history` / `nextRunAt` 更新。
5. 查看 `getAgentDir()/scheduled-tasks.json`，确认持久化正确。
